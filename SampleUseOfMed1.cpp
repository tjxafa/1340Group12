#include "med1.h"

int main() {


    bool result = playMed1();

    // the function playMed1() will return true if the player wins, and return false if the player fails. thus bool result can store the result of game tic-tac-toc
    
    return 0;
}
